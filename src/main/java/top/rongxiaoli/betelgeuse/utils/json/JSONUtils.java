package top.rongxiaoli.betelgeuse.utils.json;

// Todo
public class JSONUtils {
    /**
     * JSON file name and its full path.
     */
    public String jsonFullPath;

    public JSONUtils(String jsonFullPath) {
        this.jsonFullPath = jsonFullPath;
    }
}
